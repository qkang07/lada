import SidePane from '@/components/SidePane'
import React from 'react'

type Props = {}

const CompSlots = (props: Props) => {
  return (
    <SidePane title='插槽'>CompSlots</SidePane>
  )
}

export default CompSlots