import SidePane from '@/components/SidePane'
import React from 'react'

type Props = {}
const CompActions = (props: Props) => {
  return (
    <SidePane title='行为'>CompActions</SidePane>
  )
}

export default CompActions