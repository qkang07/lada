import { UIComp } from '@/libs/core/Def'
import React from 'react'

type Props = {
  schema: UIComp.Schema
}

const DataSourceEditor = (props: Props) => {
  
  return (
    <div>

    </div>
  )
}

export default DataSourceEditor